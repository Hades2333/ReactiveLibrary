    import XCTest
    @testable import ReactiveNetwork_library

    final class ReactiveNetwork_libraryTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ReactiveNetwork_library().text, "Hello, World!")
        }
    }
